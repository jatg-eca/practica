/*El loop for tiene tres parámetros
*inicialización* (marcando o creando una variable/índice "i")
*condición* (mientras que "i" sea menor o igual 5...)
*y acción* que realiza al final (...se va a sumar...)*/

for (let i=0; i <= 5; i=i+2) {
    window.alert(`Se va a imprimir el índice ${i}`);
}